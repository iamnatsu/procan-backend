{
  "name": "procan",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "serve": "tsc && node ./build/index.js",
    "debug": "tsc && node --inspect-brk ./build/index.js",
    "docker:run": "docker-compose -f ./docker-compose/docker-compose.yml up",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "hapi": "^17.5.5",
    "mongodb": "^3.1.6"
  },
  "devDependencies": {
    "@types/mongodb": "^3.1.9",
    "@types/node": "^10.10.3",
    "typescript": "^2.9.2"
  }
}
