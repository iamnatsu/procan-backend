import { routing } from '../route';

// to activate decorator...
const userService = require('./user-service');
const projectService = require('./project-service');
const taskService = require('./task-service');
routing();